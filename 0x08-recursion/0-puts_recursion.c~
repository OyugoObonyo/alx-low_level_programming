#include "holberton.h"

/**
 *_puts_recursion-Prints a string followed by new line
 *@s:Parameter
 * Return:Void
 */

void _puts_recursion(char *s)
{
	if (*s == '\0')
	{
		return;
	}
	else
	{
		_putchar(*s);
		_puts_recursion(s + 1);
	}
}
